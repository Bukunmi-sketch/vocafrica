steps:
  # Build and tag for PROD
  - name: 'gcr.io/cloud-builders/docker'
    args:
      [
        'build',
        '--build-arg', 'ENV=production',
        '-t', 'gcr.io/telepracticepro-prod/telepracticepro-frontend:latest',
        '.'
      ]

  # Push PROD image
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/telepracticepro-prod/telepracticepro-frontend:latest']

  # Deploy to PROD Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      [
        'run', 'deploy', 'telepracticepro-frontend',
        '--image=gcr.io/telepracticepro-prod/telepracticepro-frontend:latest',
        '--region=us-east1',
        '--platform=managed',
        '--allow-unauthenticated'
      ]

timeout: 1800s

options:
  logging: CLOUD_LOGGING_ONLY

images:
  - 'gcr.io/telepracticepro-prod/telepracticepro-frontend:latest'
