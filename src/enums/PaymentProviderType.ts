/* eslint-disable no-unused-vars -- enum members are used via PaymentProviderType.* elsewhere */
export enum PaymentProviderType {
  ALEHRA_PAYMENT = "alehra-payment",
  STRIPE = "stripe",
  PAYPAL = "paypal",
  AUTHORIZE_NET = "authorize-net",
  MANUAL = "manual",
}
