/* eslint-disable no-unused-vars -- enum members are used via Role.* elsewhere */
export enum Role {
  ADMINISTRATOR = "Administrator",
  SCHEDULER = "Scheduler",
  BILLER = "Biller",
  PROVIDER = "Provider",
  SUPERVISOR = "Supervisor",
  PROVIDER_ADMINISTRATOR = "Provider Administrator",
}
